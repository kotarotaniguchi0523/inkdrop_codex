{
  "name": "inkdrop-codex-workspace",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/kotarotaniguchi0523/inkdrop_codex.git"
  },
  "packageManager": "pnpm@11.15.1",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "build:helper": "pnpm --filter @inkdrop-codex/credential-helper build",
    "biome:check": "biome check .",
    "biome:fix": "biome check --write .",
    "check": "pnpm -r check",
    "perry:check": "pnpm --filter @inkdrop-codex/credential-contract build && pnpm --filter @inkdrop-codex/credential-helper exec perry check --check-deps --deep-deps --strict src/main.ts",
    "package:release": "pnpm --filter inkdrop-codex package:release",
    "quality:ci": "pnpm biome:check && pnpm check && pnpm test && pnpm perry:check && pnpm --filter inkdrop-codex build",
    "quality": "pnpm workflows:lint && pnpm biome:check && pnpm check && pnpm test && pnpm perry:check && pnpm --filter inkdrop-codex build",
    "test": "pnpm -r test",
    "verify": "pnpm check && pnpm test && pnpm build",
    "workflows:lint": "sh scripts/lint-workflows.sh"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.3",
    "@types/node": "^26.1.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
